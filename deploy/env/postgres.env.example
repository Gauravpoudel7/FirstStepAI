# FirstStep AI — Postgres runtime config (consumed by docker-compose).
POSTGRES_DB=firststepai
POSTGRES_USER=firststepai
POSTGRES_PASSWORD=firststepai

# Web port mapping (host:container).
WEB_PORT=8080